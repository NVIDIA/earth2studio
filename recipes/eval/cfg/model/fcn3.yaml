# FCN3 (FourCastNet 3) — probabilistic spherical ML weather model
# 6-hour time step, 721x1440 lat/lon grid, 72 prognostic variables
# Built-in stochastic noise for ensemble diversity
architecture: earth2studio.models.px.FCN3
# Omit package_path to use the default HuggingFace checkpoint.
# package_path: /path/to/custom/checkpoint
