# Local anonymous MQTT broker for the opt-in broker integration tests (test_broker_integration.py).
# NOT for production — no auth, no TLS. See that test module's docstring for how to run.
listener 1883 0.0.0.0
allow_anonymous true
