# Core dependencies for Earth2Studio Python Client SDK
requests>=2.25.0
urllib3>=1.26.0
aiohttp>=3.8.0   # For longer timeouts when reading remote zarr (fsspec HTTP)

# Optional dependencies for enhanced functionality
# Uncomment if needed for your use case:
pandas>=1.3.0         # For data manipulation
numpy>=1.21.0         # For numerical operations
xarray>=0.19.0        # For working with multidimensional arrays
matplotlib>=3.3.0     # For plotting and visualization
omegaconf>=2.3.0      # For StormCast and other models that use OmegaConf
nvidia-physicsnemo>=1.0.1,<2.0  # For StormCast and other physicsnemo-based models
pytz                 # Required by nvidia-physicsnemo
